name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write


jobs:
  changes:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    outputs:
      web: ${{ steps.select.outputs.web }}
      frontend: ${{ steps.select.outputs.frontend }}
      infra: ${{ steps.select.outputs.infra }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v6
      - name: Get changed files
        id: diff
        uses: tj-actions/changed-files@ed68ef82c095e0d48ec87eccea555d944a631a4c # v46
        with:
          json: true
      - name: Run selective CI
        id: select
        env:
          CHANGED_FILES: ${{ steps.diff.outputs.all_changed_files }}
        run: uv run --no-project python .github/scripts/selective_ci.py --changed-files-json "$CHANGED_FILES" --github-output "$GITHUB_OUTPUT"

  compliance:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v5
      - name: Run compliance checks
        run: ./.github/scripts/run-compliance.sh

  ci-scripts:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v5
        with:
          sparse-checkout: .github/scripts
      - uses: astral-sh/setup-uv@v6
      - name: Test selective CI
        run: uv run --with pytest pytest .github/scripts/test_selective_ci.py

  web:
    needs: changes
    if: needs.changes.outputs.web == 'true'
    uses: ./.github/workflows/web.yml
    with:
      event_name: ${{ github.event_name }}
    secrets:
      KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}

  frontend:
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    uses: ./.github/workflows/frontend.yml
    with:
      event_name: ${{ github.event_name }}

  infra:
    needs: changes
    if: needs.changes.outputs.infra == 'true'
    uses: ./.github/workflows/infra.yml
    with:
      event_name: ${{ github.event_name }}
