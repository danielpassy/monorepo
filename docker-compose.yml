services:
  base:
    image: monorepo-python-base:dev
    build:
      context: .
      dockerfile: docker/base.Dockerfile

  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: monorepo
      POSTGRES_PASSWORD: monorepo
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./local/initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U monorepo"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  web:
    image: monorepo-web:dev
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      additional_contexts:
        base: service:base
      args:
        BASE_IMAGE: base
    environment:
      DATABASE_URL: postgresql+asyncpg://monorepo:monorepo@postgres:5432/monorepo
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: local-dev-secret-key
    ports:
      - "8001:8000"
    depends_on:
      postgres:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres-data:
