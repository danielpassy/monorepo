# Hetzner API token
TF_VAR_hcloud_token=your-hetzner-api-token

# IPs allowed to SSH into the servers (JSON list)
TF_VAR_ssh_allowed_ips=["YOUR_IP/32"]

# S3 backend credentials (Hetzner Object Storage)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
